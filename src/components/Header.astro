---
const links = [
	{
		name: 'Home',
		path: '/'
	},
	{
		name: 'Todos',
		path: '/todos'
	}
]
---
<header class="flex justify-between items-center px-60 py-3 bg-gray-300" >
	{links.map((item) => (<a href={item.path}>{item.name}</a>))}
</header>